# Custom Postgres configuration for the local Docker workflow.
listen_addresses = '*'
port = 5432

# Safe basic tuning for a demo-sized environment.
max_connections = 100
shared_buffers = 256MB
work_mem = 8MB
